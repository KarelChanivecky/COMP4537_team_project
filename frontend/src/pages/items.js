import React from 'react';
import {Grid, List, Typography} from "@material-ui/core";

function Items(props) {
    return (
        <Grid container direction="column">
            <Typography variant="h3">TODO Items</Typography>
            <List>
                {/*    TODO */}
            </List>
        </Grid>
    );
}

export default Items;